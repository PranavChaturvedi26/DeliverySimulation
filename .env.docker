# Docker Environment Variables for Development
NODE_ENV=development
PORT=5000

# Database Configuration (Docker internal network)
MONGO_URI=mongodb://admin:admin123@mongodb:27017/greencart?authSource=admin

# JWT Configuration
JWT_SECRET=docker_development_jwt_secret_change_in_production

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Redis Configuration (Docker internal network)
REDIS_HOST=redis
REDIS_PORT=6379

# Cache Configuration
CACHE_TTL=300